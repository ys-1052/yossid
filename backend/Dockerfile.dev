FROM golang:1.26-alpine

WORKDIR /app

# Copy dependency files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code (will be overridden by volume mount in docker-compose, but useful for build)
COPY . .

EXPOSE 8080

CMD ["go", "run", "cmd/server/main.go"]
